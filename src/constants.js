// Storage keys
const STORAGE_KEYS = {
  FORMATTER_CONTENT: 'formatterContent',
  FORMAT_HISTORY: 'formatHistory',
  TIMESTAMP: 'timestamp'
};

// Format types
const FORMAT_TYPES = {
  JSON: 'json',
  XML: 'xml'
};

// Context menu IDs
const MENU_IDS = {
  FORMAT_AND_VIEW: 'format-and-view'
};
